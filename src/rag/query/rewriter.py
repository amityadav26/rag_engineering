# | # | Human Query                                                        | LLM-Rewritten Query                                                                                                                       | Expected Documents              |
# | - | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
# | 1 | **Can I work from home a few days a week?**                        | **What is the company policy for the maximum number of days employees can work remotely per week?**                                       | `doc_001`                       |
# | 2 | **Why am I getting that connection reset thing?**                  | **What causes ERR_CONNECTION_RESET, and what does it mean when a network connection is unexpectedly closed?**                             | `doc_002`, `doc_036`            |
# | 3 | **Who has to say yes before we push something live?**              | **Who is required to approve a production deployment before it is released to production?**                                               | `doc_003`                       |
# | 4 | **How do I make my database queries faster?**                      | **What techniques can improve database query performance and allow records to be retrieved more efficiently?**                            | `doc_019`                       |
# | 5 | **How does this RAG search find stuff that means the same thing?** | **How does semantic search retrieve documents that are semantically similar even when the query uses different words from the document?** | `doc_030`, `doc_042`, `doc_045` |
